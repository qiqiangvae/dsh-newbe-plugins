{
  "name": "dsh-newbe-plugins",
  "version": "0.0.0",
  "private": true,
  "description": "newbe 品牌的 DeepSeek Harness (DSH) Web 插件合集（pnpm workspace monorepo）",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "check": "pnpm -r run check",
    "test": "pnpm -r run test"
  }
}
