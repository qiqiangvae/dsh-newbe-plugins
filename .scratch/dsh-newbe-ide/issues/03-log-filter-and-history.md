# 03: 日志能用：过滤 + 落盘历史

**What to build:** 日志区能按关键字（含正则）、按 level（ERROR/WARN/INFO/DEBUG/OTHER）、按"仅看匹配"过滤，且过滤状态下仍然自动跟随；日志同时落盘，DSH 重启后还能翻到上一次跑的输出。

**Blocked by:** 02 (一条启动配置能一键启停，日志实时滚出来)

**Status:** resolved（验收项全部达成，见 Comments）

- [x] 关键字过滤即时生效；「正则」开关切换后按正则解释，非法正则退回字面匹配且不报错
- [x] `仅看匹配` / `全部高亮` 两种模式可切换（命中行整行淡黄底）
- [x] level 徽章多选，任一关闭后对应行立即消失
- [x] 过滤开启时新到达的匹配行仍自动跟随；不匹配的行不打断跟随
- [x] 单条启动配置的日志达到环形上限后从头部丢弃，滚动不卡（环形 5000 行 + 单帧渲染封顶 2000 行）
- [x] 日志同时追加落盘到 `$DSH_HOME/storages/dsh-newbe-ide/logs/<项目>_<配置>.log`；DSH 重启后能读回上一次的输出（保留一代 `.1`，单文件上限 8MB）
- [x] 面板显示「显示 M / 共 F 行（缓存 N 行）」，补历史时标「含上次运行的输出」，截断有提示

## Comments

**实现记录（2026-09-12）**

- **过滤是纯函数**（`src/filter.ts`）：级别识别（FATAL→ERROR、TRACE→DEBUG）、关键字/正则匹配、过滤结果带 `hit` 标记。9 项测试覆盖大小写、非法正则退回、级别多选、空关键字、不改动原数组。
- **落盘**（`src/logsink.ts`）：每条启动配置一个文件，键经消毒（`../` 之类写不出去），追加写入、读尾部（只读最后 512KB）、超 8MB 轮转成 `.1`（只留一代：够回答"上次为什么挂"，又不会吃满磁盘）。6 项测试。
- **写入时机**：一次 drain 批一次写（不是一行一次），落盘失败不影响内存日志流。
- **历史读回**：内存缓冲为空且本轮进程没有输出时，客户端捞回落盘尾巴（2000 行）并标注「含上次运行的输出」；`start` 之后不再补历史（新进程输出从零开始）。宿主侧有端到端测试：进程输出 → drain → 落盘 → 新建 sink（模拟重启）仍读回同一份。
- **渲染封顶**：过滤是全量的，单帧最多渲染 2000 行，避免长日志拖慢面板。
- **一个用户可见的决定**：视图里出现了「过滤关键字」输入框。此前用户说过视图不该有输入控件，但那条针对的是配置表单与底部消息框；控制台搜索框是常规交互，已在提交信息与回复里说明，若用户不要可退化成只留级别徽章。

**Standards 轴评审处理（2026-09-12）**

| 评审发现 | 处理 |
| --- | --- |
| 级别词表在三处重复（`RunLevel`、默认表、客户端徽章数组、CSS 类），且 INFO 没有配色规则 | `filter.ts` 导出有序 `LEVELS`，客户端徽章从它派生；补 `.ide-lv-INFO` |
| `Matcher.literal` 无生产代码读取（推测性通用 + 命名绕） | 删除该标记；保留"非法正则退回字面匹配"的行为，测试改为行为断言（含能匹配到的正向用例） |
| 历史行数 `2000` 在宿主与客户端各写一份 | 下沉为 `schema.ts` 的 `DEFAULT_HISTORY_LINES`，两端共用 |
| `drain()` 两处出口各调一次 `flushAppend`，将来加早退会静默丢批次 | 拆出 `drainInto`，`drain` 用 `try/finally` 只在一处落盘 |
| 宿主服务签名手写 `{ workspaceId, configId, tail }` 字段簇 | 改用共享类型 `LogHistoryRequest` |
| 票状态仍是 `ready-for-agent`（已勾完并写了实现记录） | 改为 `resolved` |

**Spec 轴评审处理（2026-09-12）**

| 评审发现 | 处理 |
| --- | --- |
| `tail` 的边界处理会丢掉一条**完整行**（起点正好落在行首时仍无条件 `shift`） | 改为多读一个字节判断起点前一字节是否换行；补测试（20 条等长行、`tailBytes` 正好切在第 10 条行首） |
| `.1` 写了却从不读——轮转掉的那一代才是"上次为什么挂"所在 | `tail` 先读新一代、不足再补上一代；补两条测试（轮转一次两代都在；第二次轮转会覆盖更早一代，这是"只留一代"的设计） |
| 历史与本次运行的输出共用一个缓冲：进程在跑但还没输出时挂载面板，会把上一条的尾巴标成「含上次运行的输出」再追加新输出 | 本进程第一行到达时历史整段让位，并清掉标记（`fromHistoryRef`） |
| 级别识别是**大小写敏感的子串**匹配：`error:`（npm/node/go）落到 OTHER，`com.x.ErrorHandler` 反被判成 ERROR | 改为独立词匹配（`\b`）+ 小写兜底；补测试（小写 error/warn 命中、`ErrorHandler` 与 `INFORMATION_SCHEMA` 不误判） |
| 单帧渲染上限静默截掉头部，`显示 2000 / 共 3800 行` 不说明原因 | 状态行补「仅渲染最近 2000 行」；历史截断的措辞与"丢弃"区分开（「只取了最近一段」） |
| 日志文件名用不透明 id（票里写的是 `<项目>_<配置>`），且"消毒替换"会让 `a/b` 与 `a_b` 撞车 | 改为百分号编码：可逆、不撞车；补测试（两个键两个文件） |
| `DEFAULT_LEVELS.DEBUG=false` 会在未做任何过滤时隐藏 DEBUG（票里只要求"关掉徽章才隐藏"） | 默认全部打开——不主动隐藏任何输出 |
